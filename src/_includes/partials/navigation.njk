{% set navPages = collections.pages | eleventyNavigation %}
<!-- Navbar Start -->
<nav class="navbar navbar-expand-lg bg-white navbar-light sticky-top py-0 pe-5">
    <a href="index.html" class="navbar-brand ps-5 me-0">
        <h1 class="text-white m-0">Industro</h1>
    </a>
    <div>
        <button type="button" class="navbar-toggler me-0" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div
            class="collapse navbar-collapse" id="navbarCollapse">
            {# <ul role="list" class="navbar-nav ms-auto p-4 p-lg-0"> #}
            {%- for entry in navPages %}
                {# <li {% if entry.url == page.url %} class="sub-menu active-page" {% endif %}> #}
                <a href="{{ entry.url }}" class="nav-item nav-link">{{ entry.title }}</a>
                {# </li> #}
            {%- endfor %}
            {# </ul> #}
            <a href=" " class=" btn btn-primary px-3 d-none d-lg-block">Get A Quote</a>
        </div>
    </div>
</nav>
<!-- Navbar End -->