---
layout: base.njk
---

<article>
    <h1>{{title}}</h1>
    <img src="{{featuredImage}}" alt="{{featuredImage}}">
    <p>{{ content | safe }}</p>
    {# <p>Publié par {{ author }} le {{ post.date | date }}</p> #}
</article>